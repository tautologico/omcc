#include <stdio.h>

// calcula a soma de tres numeros
int soma(int a, int b, int c)
{
   a = a + b + c;
   return a;
}

int main()
{
   // calcula o discriminante da eq. de segundo grau x^2 - 3x + 2
   printint(soma(10, -3, 21));
   return 0;
}
